# Frontend skeleton (Platform 3.0)

Minimal Freshdesk Support Ticket sidebar app with Crayons. Replace placeholders and extend `app/scripts/app.js` as needed.

## Setup

Install in Freshdesk, open a ticket with `?dev=true`, and use **fdk run** from the app root during development.
