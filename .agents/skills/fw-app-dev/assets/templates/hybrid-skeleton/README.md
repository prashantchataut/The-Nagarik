# Hybrid skeleton (Platform 3.0)

Ticket sidebar UI plus SMI (`fetchExternalData`) and a demo `POST` request template to `httpbin.org` (replace with your API). Swap `externalApiCall` for production hosts and add OAuth if needed (`references/playbooks/`).
