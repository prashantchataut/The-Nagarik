---
description: Fix async/await patterns for FDK validation. Use when fdk validate shows "Async function has no 'await' expression", async scoping issues, or unhandled promise rejections. Covers IIFE pattern, error handling, and race conditions.
alwaysApply: false
globs: ["**/server/**/*.js", "**/app/**/*.js"]
---

# Async/Await Patterns for FDK Validation

**When to use:** `fdk validate` shows async-related errors

## Common Async Errors

### Error 1: Async without await

```
Lint Error: Async function has no 'await' expression
```

**Fix:** Remove `async` keyword OR add actual `await`

```javascript
// [INVALID] WRONG: async without await
exports = {
  onAppInstallHandler: async function() {
    console.log('Installed');
  }
};

// [VALID] CORRECT: Remove async
exports = {
  onAppInstallHandler: function() {
    console.log('Installed');
  }
};

// [VALID] CORRECT: Add await
exports = {
  onAppInstallHandler: async function() {
    await $db.set('installed', { timestamp: Date.now() });
    console.log('Installed');
  }
};
```

### Error 2: Async Variable Scoping (Frontend)

```
Warning: Async function may cause race conditions in frontend initialization
```

**Fix:** Use IIFE pattern

```javascript
// [INVALID] WRONG: Top-level async causes race conditions
async function init() {
  const client = await app.initialized();
  // ... rest of code
}
init();

// [VALID] CORRECT: IIFE pattern
(async function() {
  const client = await app.initialized();
  // ... rest of code
})();
```

### Error 3: Unhandled Promise Rejection

```
Warning: Expected rejection to be handled
```

**Fix:** Wrap in try/catch

```javascript
// [INVALID] WRONG: No error handling
exports = {
  fetchData: async function() {
    const result = await $request.invokeTemplate('api', {});
    return result;
  }
};

// [VALID] CORRECT: Try/catch wrapper
exports = {
  fetchData: async function() {
    try {
      const result = await $request.invokeTemplate('api', {});
      return { success: true, data: result };
    } catch (e) {
      console.error('API call failed:', e.message);
      return { success: false, error: e.message };
    }
  }
};
```

## Frontend Async Patterns

### Pattern 1: App Initialization (IIFE)

```javascript
// [VALID] CORRECT: Standard frontend pattern
(async function() {
  const client = await app.initialized();

  document.getElementById('btn').addEventListener('fwClick', async () => {
    try {
      const response = await client.request.invoke('getData', {});
      displayResult(response);
    } catch (e) {
      showError(e.message);
    }
  });
})();
```

### Pattern 2: Multiple Async Calls

```javascript
// [VALID] CORRECT: Sequential when order matters
const user = await client.data.get('loggedInUser');
const ticket = await client.data.get('ticket');

// [VALID] CORRECT: Parallel when independent
const [user, ticket] = await Promise.all([
  client.data.get('loggedInUser'),
  client.data.get('ticket')
]);
```

## Backend Async Patterns

### Pattern 1: SMI Function

```javascript
// [VALID] CORRECT: SMI MUST use renderData (never return as the client response)
exports = {
  getData: async function(args) {
    try {
      const result = await $request.invokeTemplate('externalAPI', {
        body: JSON.stringify(args)
      });
      renderData(null, { success: true, data: result.response });
    } catch (e) {
      console.error('External API failed:', e.message);
      renderData({ status: 500, message: e.message });
    }
  }
};
```

### Pattern 2: Event Handler

```javascript
// [VALID] CORRECT: Event handler with async operations — complete with renderData()
exports = {
  onTicketCreateHandler: async function(args) {
    const ticket = args.data.ticket;

    try {
      await $request.invokeTemplate('notifySlack', {
        body: JSON.stringify({ text: `New ticket: ${ticket.subject}` })
      });
      console.log('Notification sent for ticket:', ticket.id);
      renderData();
    } catch (e) {
      console.error('Notification failed:', e.message);
      renderData();
    }
  }
};
```

### Pattern 3: Multiple External Calls

```javascript
// [VALID] CORRECT: Sequential SMI calls — end with renderData
exports = {
  getUserPresence: async function(args) {
    try {
      const userResponse = await $request.invokeTemplate('getUserByEmail', {
        body: JSON.stringify({ email: args.email })
      });

      const userId = userResponse.response.id;

      const presenceResponse = await $request.invokeTemplate('getPresence', {
        body: JSON.stringify({ userId })
      });

      renderData(null, { success: true, presence: presenceResponse.response });
    } catch (e) {
      renderData({ status: 500, message: e.message });
    }
  }
};
```

## Critical Rules

1. **Only use `async` if function contains `await`** (lint error otherwise)
2. **Use IIFE pattern in frontend** `(async function() { ... })()`
3. **Always wrap async operations in try/catch** (prevent unhandled rejections)
4. **SMI responses:** use `renderData(null, payload)` / `renderData(error)` — not `return` for the client
5. **Return structured responses** `{ success: boolean, data?: any, error?: string }`
6. **Log errors with `e.message` only** (not full error objects) — See **security.mdc** for error logging rules

## Common Mistakes

### Mistake 1: Forgetting await

```javascript
// [INVALID] WRONG: Missing await (returns Promise, not value)
const data = client.data.get('ticket'); // Promise object!

// [VALID] CORRECT: Use await
const data = await client.data.get('ticket');
```

### Mistake 2: Async in wrong context

```javascript
// [INVALID] WRONG: Can't use await in non-async function
function getData() {
  const result = await $request.invokeTemplate('api', {}); // Error!
}

// [VALID] CORRECT: Make function async
async function getData() {
  const result = await $request.invokeTemplate('api', {});
}
```

### Mistake 3: Not handling errors

```javascript
// [INVALID] WRONG: Unhandled rejection
button.addEventListener('fwClick', async () => {
  const data = await client.request.invoke('getData', {}); // May fail!
});

// [VALID] CORRECT: Try/catch wrapper
button.addEventListener('fwClick', async () => {
  try {
    const data = await client.request.invoke('getData', {});
    displayData(data);
  } catch (e) {
    showError(e.message);
  }
});
```

## Related Patterns

- For complexity → See `complexity-reduction.mdc`
- For security → See `security.mdc`
- For validation → See `validation-workflow.mdc`
- For logging → See `logging-debugging.mdc`